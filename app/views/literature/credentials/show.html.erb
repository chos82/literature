<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @credential.user_id %>
</p>

<p>
  <strong>Token:</strong>
  <%= @credential.token %>
</p>

<%= link_to 'Edit', edit_credential_path(@credential) %> |
<%= link_to 'Back', credentials_path %>
